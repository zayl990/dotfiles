dotfiles
========

``` bash
# set zsh as your login shell
chsh -s /bin/zsh

# clone repo
git clone git@github.com:olownia/dotfiles.git

# create symlinks for config files in your home directory
cd dotfiles && ./install.sh
```